// перевод данных в RSI из градусов по Фарингейту, к градусам по Цельсию
subq $32, %RSI
movq $5, %RAX 
mulq %RSI // оператор умножает значени этого регистра, на регистр RAX и записывает в RAX первую часть, в RDX вторую
movq $9, %RSI
divq %RSI //Делит RDX*2^64 + RAX на RSI и сохраняет результат от деления в RAX, а остаток в RDX 
movq %RAX, %RSI